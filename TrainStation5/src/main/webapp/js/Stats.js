var chart = c3.generate({
    bindto: '#perDay',
    data: {
      columns: [
        ['Packages', 30, 200, 100, 400, 150, 250,230]
      ]
    }
});

var chart = c3.generate({
    bindto: '#perMonth',
    data: {
      columns: [
        ['Packages', 30, 200, 100, 400, 150, 250,230]
      ]
    }
});

var chart = c3.generate({
    bindto: '#perYear',
    data: {
      columns: [
        ['Packages', 30, 200, 100, 400, 150, 250,230]
      ]
    }
});